package Java_Basics.String;

public class ReturnStringLength {
    public static int returnStringLength(String input) {
        int result = Integer.parseInt(String.valueOf(input.length()));
        return result;
    }
}
